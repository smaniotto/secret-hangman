import React from "react";

import styles from "./styles.module.scss";

const Footer = () => {
  return (
    <nav className={styles.container}>
      <p className={styles.title}>powered by secret network</p>
    </nav>
  );
};

export default Footer;
